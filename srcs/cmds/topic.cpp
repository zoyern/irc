#include <irc.hpp>

void topic(int fd, std::string reason)
{
	// verify if channel exist
	// verify if user is in channel
	// verify topic restriction
		// then change topic name
	// broadcast to all user of the channel
}
